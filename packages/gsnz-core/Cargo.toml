[package]
name = "gsnz-core"
version = "0.0.0"
edition = "2021"
description = "Retailer-neutral grocery domain types shared by the NZ supermarket CLIs"
license = "MIT"
publish = false

[dependencies]
async-trait = "0.1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
