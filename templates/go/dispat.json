{
  "scripts": {
    "build": "go build -ldflags \"-X main.version=${DISPAT_NEW_VERSION:-0.0.0-dev}\" -o bin/__NAME__ .",
    "run": "go run .",
    "test": "go test ./...",
    "fmt": "gofmt -w .",
    "fmt-check": "out=$(gofmt -l .); if [ -n \"$out\" ]; then echo \"needs gofmt:\"; echo \"$out\"; exit 1; fi",
    "lint": "go vet ./...",
    "check": [
      "out=$(gofmt -l .); if [ -n \"$out\" ]; then echo \"needs gofmt:\"; echo \"$out\"; exit 1; fi",
      "go vet ./...",
      "go build -o bin/__NAME__ .",
      "go test ./..."
    ]
  }
}
