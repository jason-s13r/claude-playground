{
  "scripts": {
    "build": "make build VERSION=\"${DISPAT_NEW_VERSION:-0.0.0-dev}\"",
    "run": "make run",
    "test": "make test",
    "fmt": "make fmt",
    "fmt-check": "make fmt-check",
    "check": [
      "make fmt-check",
      "make build VERSION=\"${DISPAT_NEW_VERSION:-0.0.0-dev}\"",
      "make test"
    ]
  }
}
